# input for postprocessing dvmc (dvmc.out with NVMCCalMode == 3)
# '#' are treated as comment

# parameters to generate excitation.def file:
dr1_x       0:1      # will generate array [-1,0,1,2]
dr1_y       0:1      # will generate array [-1,0,1,2]
dr2_x       0:1      # will generate array [-1,0,1,2]
dr2_y       0:1      # will generate array [-1,0,1,2]

# parameters to generate A(k,w) parameters 
eta           0.2             # imaginary frequency
w_min        -8.0             # minimum frequency plotted
w_max        12.0             # maximum frequency plotted
w_min_data  -15.0             # minimum frequency calculated
w_max_data   15.0             # maximum frequency calculated
Nw           1500             # calculate 1500 frequencies
exc_choice   all              # take all excitations
kPath        0,5,10,6,2,1,0   # k points chosen in the path to plot

# geometry of the sites (both real space and reciprocal k-space):
#
# 12 13 14 15
#  8  9 10 11
#  4  5  6  7
#  0  1  2  3


